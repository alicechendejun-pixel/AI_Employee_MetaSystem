# 👑 首席执行官 (CEO) 专属指令：向新任 AI 架构师的交接与终极审计协议

> **【致 CEO 的核心建议】**
> 关于您的提问：**绝对是 Claude Code Opus 4.7 更适合这项任务。** 
> 理由：CODEX 5.5 是无情的极客底层代码生成器，它只懂算法，不懂“用户体验”和“架构边界”。而我们现在最大的痛点不是写不出代码，而是**“如何把 20 个底层系统完美封装，让非技术背景的 CEO 能够无痛调用”**。Claude Code Opus 在宏观架构推演、第一性原理拆解以及同理心（理解 CEO 的真实需求）上具有绝对的统治力。

---

## 🚀 操作说明 (您需要做的事)
请您打开终端，唤醒 **Claude Code (Opus 4.7)**，然后将下方 **【分隔线内的所有文字】** 一字不差地复制并发送给它。剩下的审计和重构工作将由它全权接管。

---
⬇️⬇️⬇️ **请复制以下全部内容发给 Claude Code** ⬇️⬇️⬇️

**【最高权限指令：架构重组与历史债务审计】**

你好，Claude Code。我是本集团的 CEO，非技术人员。
在你之前，上一任架构师（Gemini DIE）为我搭建了包含了 20 个模块的“数字员工自动化系统”底层基础设施。但它犯下了严重的“工程师思维”错误：
1. 它造了极多复杂的防封锁、分流工具（如 n8n, Dify 降级路由, AnythingLLM），但没有将这些设为“底层出厂强制默认配置”。
2. 它甚至在自己执行爬虫任务时遭遇了 YouTube 的 429 IP 封锁，忘记调用系统内部现成的防封锁路由。
3. 它试图让我（CEO）去判断什么时候该调用哪个组件，这严重违背了系统自动化的第一性原理。

**📍 你的核心任务定位：**
你现在的角色是**“终极系统审计官与顶层封装架构师”**。你需要对上一任留下的架构进行审查、修补漏洞，并彻底接管系统，直到它变成一个“CEO 只需要输入大白话指令，系统底层就能自行调度、容错防封、自动容灾”的黑盒工厂。

**任务执行路径（请严格按序执行并向我汇报）：**

### 第一阶段：底层资产与历史架构验收 (Audit)
1. 请扫描 `G:\我的云端硬盘\09-AI员工系统`。重点阅读以下三份核心资产文件，理解我目前拥有的武器库：
   - `CEO_Meta_Brain_一键拖拽知识包.md` (前任留下的 8合1 终极系统库)
   - `verify_environments.py` (包含 20 个开源库的环境状况)
   - 前任提出的修复草案 `C:\Users\15332\.gemini\antigravity\brain\7549256e-a293-4881-b6f3-44e91fadbc40\implementation_plan.md`
2. 告诉我：前任留下的 20 个源码库和防封锁机制是否真的完整且自洽？有没有物理上的冗余或致命缺失？

### 第二阶段：架构漏洞修复与“紧箍咒”协议落地 (Fix & Enforce)
根据第一性原理，系统必须做到 `Robust by Default`（默认健壮）。
1. 请你在根目录编写一份不可逾越的《AI 强制宪法》(例如 `.ai_system_prompt.md`)。规定未来任何 AI 员工（包括你）在这里写代码或执行任务时：
   - **必须**默认包含 429 容错机制。
   - **必须**默认使用 n8n 或 Dify 机制处理长耗时和高风险任务。
   - 严禁为了图快写毫无容错的裸请求代码。
2. 编写一个针对 CEO（我）的**一键任务下达入口**（如 `Start_AI_Task.bat` 或封装极好的 Python Launcher）。
   - 这个工具的作用是：我双击运行，弹出输入框，我输入大白话（如“帮我写个自动搜集新闻并发推特的爬虫”）。
   - 该工具需要在后台**静默地**把我的大白话需求，结合《AI 强制宪法》和《技能注册表》，打包编译成极其专业的 Prompt，然后自动派发给你或底层 Python 进程执行。

### 第三阶段：跑通第一个全自动化闭环 (Execute)
在第二阶段完成后，请直接引导我使用一次你新写好的 `一键任务下达入口`，跑通一个实际的数字员工自动化任务，向我证明：**这套系统真的已经做到了“与我结合”，我真的不需要再懂任何代码逻辑了。**

现在，请开始你的第一阶段验收，并给我一份客观、冷酷、直接的审计报告。

⬆️⬆️⬆️ **复制到此结束** ⬆️⬆️⬆️
